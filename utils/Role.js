module.exports.Role = class Role {
    constructor(client, reaction, user) {
        this.client = client
        this.reaction = reaction
        this.user = user
    }
    add(manualrole) {
        // Add role
    }
    remove(manualrole) {
        // Remove role
    }
}